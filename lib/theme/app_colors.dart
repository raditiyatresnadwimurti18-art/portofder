import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0891B2); // Cyan 600
  static const Color primaryDark = Color(0xFF0F766E); // Teal 700
  static const Color secondary = Color(0xFF22D3EE); // Cyan 400
  static const Color accent = Color(0xFF67E8F9); // Soft cyan accent
  static const Color background = Color(
    0xFFF5FDFF,
  ); // Light cyan-tinted background
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(
    0xFF0F172A,
  ); // Deep slate for strong contrast
  static const Color textSecondary = Color(0xFF334155); // Slate gray
  static const Color textMuted = Color(0xFF64748B); // Cool muted gray
  static const Color border = Color(0xFFBEEFF8); // Soft cyan border
  static const Color shadow = Color(0x0F0F766E); // Teal-tinted shadow
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
}
